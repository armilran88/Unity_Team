using UnityEngine;

public class PlayerMove : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("한글도 잘 나오지?");
        print("정말 잘 나오나?");
        print("비쥬얼 스튜디오에서 병합을 어떻게 하지?");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
